# CLAUDE.md

このファイルは、このリポジトリで作業するAI(Claude Code)および人間の開発者向けの規約です。

## プロジェクト概要

個人投資用の日本株・米国株横断スクリーニング・比較ツール。個人が自分のPCでローカル実行することを前提とし、複数ユーザー対応・クラウドデプロイは考慮しない。

## ディレクトリ構成の方針

- `core/` : UIに依存しないロジック(データ取得・キャッシュ・スクリーニング・バリデーション等)。**必ずテスト可能な形に保つ**(Streamlitのimportに依存させない)。
- `core/providers/` : 外部データソースごとのクライアント。1ソース1ファイル。
- `app.py` / `ui/` : Streamlit UI。`core/` の関数を呼び出すだけにし、ロジックを持たない。
- `tests/` : `core/` と1対1対応するテストファイルを置く(`core/foo.py` → `tests/test_foo.py`)。
- `scripts/` : ローカル環境でのみ実行する疎通確認・運用スクリプト。

## コーディング規約

- 型ヒントを必ず付ける。`from __future__ import annotations` を使い、`str | None` 記法を使用する。
- 外部API呼び出しは例外を握りつぶさず、`core.errors.ProviderError` に理由付きで変換して送出する。無言で失敗させない。
- ログは `logging` モジュールを使う(`print` は使わない)。

## テスト方針

- **新機能・バグ修正には必ずテストを追加する。**
- 外部API(yfinance, J-Quants, Finnhub)への実通信はテストで行わない。`unittest.mock` でHTTPレスポンスをモックする。
- `pytest` がリポジトリルートでネットワーク接続なしに完走することを維持する。

## このプロジェクト固有のルール

1. **予想値には必ずラベルと出典日を付ける。** 「会社予想」「アナリスト予想」等のラベル、出典(データソース名)、更新日(as_of)を必ずセットで表示・保持する。実績値と同じ見た目で表示しない。
2. **取得できない値は空欄でなく「取得不可」(`core.models.NOT_AVAILABLE`)と明示する。** `None` のまま空白表示にしない。
3. **数値を推測で埋めない。** APIから取得できない項目やパースに失敗した項目を、他の値から計算・推測して埋めることは禁止する。取得できないなら「取得不可」とする。
4. **異常値は機械的にしか判定できないことを前提にする。** LLM(Claude)は数値の妥当性そのものを判断できないため、`core/validation.py` のルールベースチェックで検知し、警告として表示・ログするに留める。数値を勝手に補正しない。
5. **外部APIのレート制限を守る。** J-Quants: 5リクエスト/分、Finnhub: 60コール/分。`core/providers/` のクライアントにスリープ/スロットリングを実装済みなので、新しい呼び出し箇所を追加する際もこれを経由すること。
6. **APIキーは `.env` 経由でのみ扱う。** リポジトリにキーをコミットしない。テストやサンプルコードにも実キーを書かない。

## 半自律運用について

- ファイル編集時の品質ゲート(lint/型チェック/テスト)は `.claude/settings.json` の `postEdit` フックで自動実行される。
- 定期健康診断・異常検知時の自動PR運用ルールは [CONTRIBUTING.md](./CONTRIBUTING.md) を参照。
