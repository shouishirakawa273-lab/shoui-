# CONTRIBUTING

## 定期健康診断と自動PRの運用ルール

このリポジトリは GitHub Actions のスケジュールトリガー(`.github/workflows/health-check.yml`)で
毎朝テストスイートを実行し、以下を検知します。

- J-Quants / Finnhub のAPIレスポンス形式が変わっていないか(想定フィールドの欠落)
- 取得値が `core/validation.py` の妥当性チェックに引っかからないか

異常を検知した場合、Claude Codeが原因を診断して修正コミットを作成し、Pull Requestを自動生成することがあります。

### マージ運用ルール

| 変更の種類 | 運用 |
|---|---|
| データ取得ロジック(`core/providers/*`, `core/lookup.py`)の変更 | **自動マージ禁止。** 内容を人が確認してからマージする |
| 予測値の計算・表示(`core/forecast.py`, `core/comparison.py`, 予想値まわりのUI)の変更 | **自動マージ禁止。** 内容を人が確認してからマージする |
| 妥当性チェックのしきい値(`core/validation.py`)の変更 | **自動マージ禁止。** 投資判断に影響しうるため人が確認する |
| コード整形・lintの自動修正のみ | 自動マージ可 |
| 依存パッケージの更新(バージョンアップのみ、動作確認済み) | 自動マージ可 |
| ドキュメントのみの変更 | 自動マージ可 |

理由: 銘柄データの取得ロジックや予測値の扱いに誤りがあると、誤った投資判断につながる可能性があるため、
このカテゴリの変更は必ず人間のレビューを経てからマージします。

### PRレビュー時のチェックポイント

- 予想値に「会社予想」「アナリスト予想」等のラベルと出典・更新日が引き続き付与されているか
- 取得不可の項目が空欄ではなく「取得不可」と表示されているか
- 数値の推測による補完が行われていないか
- 新規/変更されたロジックにテストが追加されているか
